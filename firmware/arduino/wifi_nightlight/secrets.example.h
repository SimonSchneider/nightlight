// Copy this file to secrets.h and fill in your credentials.
// secrets.h is gitignored — never commit real values.
#pragma once

#define WIFI_SSID     "YourNetwork"
#define WIFI_PASSWORD "yourpassword"
